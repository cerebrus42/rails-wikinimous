<div class="container">
  <h2><%= @article.title %></h2>
  <%= link_to 'Edit', edit_article_path(@article)%>
  <%= link_to "Delete",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<div>
<p><%= @article.content %></p>
